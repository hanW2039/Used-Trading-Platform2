#server:
#  servlet:
#    context-path: /trading
server:
  port: 8888
spring:
  application:
    name: my
  main:
    allow-bean-definition-overriding: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_null
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/c2c?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong
    username: root
    password: qwer53231323
  thymeleaf:
    cache: false
    suffix: .html
    mode: HTML5
    encoding: UTF-8
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
  mail:
    host: smtp.sina.com
    username: hanwcommunity@sina.com
    password: 69560aa74287aece
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: false
            required: false
#  cloud:
#    nacos:
#      server-addr: localhost:8848 # nacos 服务端地址

#  mvc:
#    static-path-pattern: classpath:/resources/static/**
#  resources:
#    static-locations: classpath:/resources/static/**
mybatis:
  mapper-locations: classpath:mapping/*.xml

logging:
  level:
    root: info
    com:
      wsk: debug
      netflix:
        discovery:
          shared:
            resolver:
              aws: error
#  file: ./logs/${spring.application.name}.log
#  logback:
#    rollingpolicy:
#      max-history: 60
#  path: ./logs

xxl:
  job:
    admin:
      addresses: http://localhost:8080/xxl-job-admin
    executor:
      appname: user-trading-platform
      ip:
      port: 9999
      accessToken:
      logretentiondays: 1
      logpath:
    accessToken: default_token
